<Window x:Class="QLKT.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:QLKT.Views"
        Title="Hệ thống quản lý khen thưởng"
        Width="1280"
        Height="800"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource Brush.Background}">

    <Grid>

        <!-- ================= LAYOUT ================= -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ================= SIDEBAR ================= -->
        <StackPanel Grid.RowSpan="2"
                    Background="{StaticResource Brush.Primary}">

            <!-- Logo -->
            <TextBlock Text="QUẢN LÝ KHEN THƯỞNG"
                       Foreground="White"
                       Margin="16"
                       FontWeight="Bold"
                       FontSize="14"/>

            <!-- Menu -->
            <Button Content="🏠 Tổng quan"
                    Style="{StaticResource Button}"
                    Tag="Primary"/>

            <Button Content="👤 Quân nhân"
                    Style="{StaticResource Button}"
                    Tag="Secondary"/>

            <Button Content="🏅 Khen thưởng"
                    Style="{StaticResource Button}"
                    Tag="Secondary"/>

            <Button Content="📊 Báo cáo"
                    Style="{StaticResource Button}"
                    Tag="Secondary"/>

        </StackPanel>

        <!-- ================= HEADER ================= -->
        <Grid Grid.Column="1"
              Background="White">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <!-- Title -->
            <TextBlock Text="Tổng quan hệ thống"
                       Style="{StaticResource Text.Heading.Medium}"
                       VerticalAlignment="Center"
                       Margin="16,0"/>

            <!-- Search -->
            <TextBox Grid.Column="1"
                     Height="34"
                     VerticalAlignment="Center"
                     Margin="0,0,16,0"/>

            <!-- User -->
            <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">

                <TextBlock Text="Admin"
                           Margin="0,0,8,0"
                           VerticalAlignment="Center"/>

                <Border Width="32"
                        Height="32"
                        Background="{StaticResource Brush.Surface.Hover}"
                        CornerRadius="16"/>

            </StackPanel>

        </Grid>

        <!-- ================= CONTENT ================= -->
        <ContentControl Grid.Row="1"
                        Grid.Column="1"
                        x:Name="MainContent">

            <!-- Default View -->
            <views:DashboardView/>

        </ContentControl>

    </Grid>

</Window>
