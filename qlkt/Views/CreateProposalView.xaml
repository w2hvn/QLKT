<UserControl x:Class="QLKT.Views.CreateProposalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="24">
        <Border Background="White" CornerRadius="8" Padding="24">
            <StackPanel>
                <TextBlock Text="TẠO ĐỀ XUẤT KHEN THƯỞNG" Style="{StaticResource Text.Heading.Medium}" Margin="0,0,0,24"/>

                <!-- Search -->
                <Grid Margin="0,0,0,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Chiến sĩ được đề xuất (Mã hoặc Tên)" Style="{StaticResource Text.Caption}" Margin="0,0,0,8"/>
                    <Grid Grid.Row="1">
                        <TextBox x:Name="txtSearchSoldier" Padding="30,8,8,8" Height="36" TextChanged="txtSearchSoldier_TextChanged"/>
                        <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0" Foreground="{StaticResource Brush.Text.Muted}"/>
                        <TextBlock x:Name="lblSearchHint" Text="Tìm kiếm bằng tên hoặc mã..." VerticalAlignment="Center" HorizontalAlignment="Left" Margin="32,0" IsHitTestVisible="False" Foreground="{StaticResource Brush.Text.Muted}"/>
                    </Grid>
                </Grid>

                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Loại khen thưởng" Style="{StaticResource Text.Caption}" Margin="0,0,0,8"/>
                        <ComboBox x:Name="cboRewardCategory" Height="36" DisplayMemberPath="CategoryName" SelectedValuePath="CategoryID"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Cấp bậc hiện tại" Style="{StaticResource Text.Caption}" Margin="0,0,0,8"/>
                        <TextBox x:Name="txtRank" Height="36" IsReadOnly="True" Background="#F8F9FA"/>
                    </StackPanel>
                </Grid>

                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="Lý do đề xuất" Style="{StaticResource Text.Caption}" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtReason" Height="100" AcceptsReturn="True" TextWrapping="Wrap" Padding="8"/>
                </StackPanel>

                <Grid Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Ngày đề xuất" Style="{StaticResource Text.Caption}" Margin="0,0,0,8"/>
                        <DatePicker x:Name="dpDate" Height="36"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Đơn vị" Style="{StaticResource Text.Caption}" Margin="0,0,0,8"/>
                        <TextBox x:Name="txtUnit" Height="36" IsReadOnly="True" Background="#F8F9FA"/>
                    </StackPanel>
                </Grid>

                <!-- Attachments -->
                <TextBlock Text="Tệp đính kèm" Style="{StaticResource Text.Caption}" Margin="0,0,0,8"/>
                <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="16" Margin="0,0,0,32">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="📤" Margin="0,0,8,0"/>
                        <TextBlock Text="Kéo thả tệp hoặc tải lên" Foreground="{StaticResource Brush.Text.Muted}"/>
                    </StackPanel>
                </Border>

                <!-- Action Button -->
                <Button Content="GỬI ĐỀ XUẤT" Style="{StaticResource Button}" Tag="Dark" Height="44" HorizontalAlignment="Right" Width="150" Click="BtnSubmit_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
