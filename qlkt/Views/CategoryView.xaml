<UserControl x:Class="QLKT.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="QUẢN LÝ DANH MỤC KHEN THƯỞNG" Style="{StaticResource Text.Heading.Medium}" Margin="0,0,0,24"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- List -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Danh sách loại khen thưởng" FontWeight="Bold" Margin="0,0,0,16"/>
                    <DataGrid x:Name="dgCategories" Grid.Row="1" Style="{StaticResource DataGrid}" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="dgCategories_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding CategoryID}" Width="60"/>
                            <DataGridTextColumn Header="Tên loại" Binding="{Binding CategoryName}" Width="*"/>
                            <DataGridTextColumn Header="Mô tả" Binding="{Binding Description}" Width="1.5*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Form -->
            <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="16" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock x:Name="txtFormTitle" Text="Thêm mới danh mục" FontWeight="Bold" Margin="0,0,0,16"/>

                    <TextBlock Text="Tên loại khen thưởng" Style="{StaticResource Text.Caption}" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtName" Height="36" Margin="0,0,0,16"/>

                    <TextBlock Text="Mô tả" Style="{StaticResource Text.Caption}" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtDescription" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,24"/>

                    <Button x:Name="btnSave" Content="LƯU THÔNG TIN" Style="{StaticResource Button}" Tag="Primary" Height="40" Margin="0,0,0,12" Click="BtnSave_Click"/>
                    <Button x:Name="btnDelete" Content="XÓA DANH MỤC" Style="{StaticResource Button}" Tag="Secondary" Height="40" Foreground="Red" Margin="0,0,0,12" Visibility="Collapsed" Click="BtnDelete_Click"/>
                    <Button x:Name="btnCancel" Content="HỦY BỎ" Style="{StaticResource Button}" Tag="Secondary" Height="40" Click="BtnCancel_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
