<UserControl x:Class="QLKT.Views.RewardListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <UserControl.Resources>
        <!-- Combobox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Padding" Value="10,0,30,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" CornerRadius="4" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter x:Name="contentPresenter" Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                                    <ToggleButton x:Name="toggleButton" Grid.Column="1" ClickMode="Press" Focusable="false" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent" Padding="10">
                                                    <TextBlock Text="∨" Foreground="#999" FontSize="12" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup x:Name="popup" AllowsTransparency="true" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}">
                                <Border x:Name="dropDownBorder" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer>
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="RowHeight" Value="50"/>
            <Setter Property="ColumnHeaderHeight" Value="45"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F0F0F0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#001529"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#002b5e"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#333333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <TextBlock Text="DANH SÁCH NHÂN SỰ ĐƯỢC KHEN THƯỞNG" FontWeight="Bold" FontSize="18" Foreground="#001529" Margin="0,0,0,20"/>

        <!-- FILTER -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>

            <!-- Search box -->
            <Border Grid.Column="0" BorderBrush="#C6A87C" BorderThickness="1" CornerRadius="4" Background="White" Height="40" Margin="0,0,16,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" VerticalAlignment="Center" Margin="10,0" Foreground="#C6A87C"/>
                    <TextBox x:Name="txtSearch" Grid.Column="1" VerticalContentAlignment="Center" BorderThickness="0" Background="Transparent" TextChanged="Filter_Changed"/>
                    <TextBlock x:Name="txtSearchPlaceholder" Grid.Column="1" Text="Tìm kiếm chiến sĩ..." VerticalAlignment="Center" Foreground="#999" IsHitTestVisible="False" Margin="5,0,0,0"/>
                </Grid>
            </Border>

            <!-- Comboboxes -->
            <ComboBox x:Name="cboUnitFilter" Grid.Column="1" Margin="0,0,16,0" DisplayMemberPath="UnitName" SelectedValuePath="UnitID" SelectionChanged="Filter_Changed"/>

            <ComboBox x:Name="cboCategoryFilter" Grid.Column="2" DisplayMemberPath="CategoryName" SelectedValuePath="CategoryID" SelectionChanged="Filter_Changed"/>
        </Grid>

        <!-- DATA GRID -->
        <Border Grid.Row="2" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Background="White" ClipToBounds="True">
            <DataGrid x:Name="dgRewardList" BorderThickness="0" MouseDoubleClick="dgRewardList_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="60">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Tên Chiến Sĩ" Binding="{Binding Name}" Width="200" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Quân Hàm" Binding="{Binding Rank}" Width="120"/>
                    <DataGridTextColumn Header="Chức Vụ" Binding="{Binding Position}" Width="150"/>
                    <DataGridTextColumn Header="Đơn Vị" Binding="{Binding Unit}" Width="180"/>
                    <DataGridTextColumn Header="Danh Hiệu Đạt Được" Binding="{Binding CategoryName}" Width="*"/>
                    <DataGridTextColumn Header="Ngày Quyết Định" Binding="{Binding Date}" Width="130"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- PAGINATION -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <TextBlock x:Name="txtPaginationSummary" Text="Hiển thị 0 kết quả" VerticalAlignment="Center" Foreground="#666666"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnPrevPage" Content="Quay lại" Background="White" BorderBrush="#E0E0E0" Padding="15,8" Margin="0,0,8,0" Click="BtnPrevPage_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <StackPanel x:Name="pnlPageNumbers" Orientation="Horizontal"/>

                <Button x:Name="btnNextPage" Content="Tiếp theo" Background="#001529" Foreground="White" BorderBrush="Transparent" Padding="15,8" Click="BtnNextPage_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
