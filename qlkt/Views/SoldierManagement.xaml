<UserControl x:Class="QLKT.Views.SoldierManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: List -->
        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Tìm kiếm:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="txtSearch" Width="200" TextChanged="txtSearch_TextChanged"/>
                <Button Content="Tải lại" Margin="10,0,0,0" Click="Refresh_Click"/>
                <Button Content="Xóa" Margin="10,0,0,0" Background="Red" Click="Delete_Click"/>
            </StackPanel>

            <DataGrid x:Name="dgSoldiers" Grid.Row="1" AutoGenerateColumns="False" 
                      IsReadOnly="True" SelectionChanged="dgSoldiers_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã QS" Binding="{Binding SoldierCode}" Width="80"/>
                    <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="150"/>
                    <DataGridTextColumn Header="Cấp bậc" Binding="{Binding Rank}" Width="100"/>
                    <DataGridTextColumn Header="Chức vụ" Binding="{Binding Position}" Width="120"/>
                    <DataGridTextColumn Header="Đơn vị" Binding="{Binding UnitName}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Right: Form -->
        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Padding="10" Background="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Thông tin quân nhân" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Mã quân số:"/>
                    <TextBox x:Name="txtCode"/>
                    
                    <TextBlock Text="Họ và tên:"/>
                    <TextBox x:Name="txtName"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="Giới tính:"/>
                            <ComboBox x:Name="cboGender" Margin="0,5">
                                <ComboBoxItem Content="Nam"/>
                                <ComboBoxItem Content="Nữ"/>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                            <TextBlock Text="Cấp bậc:"/>
                            <TextBox x:Name="txtRank"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Chức vụ:"/>
                    <TextBox x:Name="txtPosition"/>
                    
                    <TextBlock Text="Đơn vị:"/>
                    <ComboBox x:Name="cboUnits" DisplayMemberPath="UnitName" SelectedValuePath="UnitID" Margin="0,5"/>
                    
                    <TextBlock Text="Dân tộc:"/>
                    <TextBox x:Name="txtEthnicity"/>
                    
                    <TextBlock Text="Tôn giáo:"/>
                    <TextBox x:Name="txtReligion"/>
                    
                    <TextBlock Text="Trình độ học vấn:"/>
                    <TextBox x:Name="txtAcademic"/>
                    
                    <TextBlock Text="Lý luận chính trị:"/>
                    <TextBox x:Name="txtPolitical"/>
                    
                    <TextBlock Text="Các trường đã học:"/>
                    <TextBox x:Name="txtSchools" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                    
                    <TextBlock Text="Quá trình công tác:"/>
                    <TextBox x:Name="txtHistory" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="Làm mới" Click="Clear_Click" Margin="0,0,10,0" Width="80"/>
                        <Button Content="Lưu" Click="Save_Click" Width="80" Style="{StaticResource Button}" Tag="Primary"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
