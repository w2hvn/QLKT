<UserControl x:Class="QLKT.Views.ApprovalProcessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="PHÊ DUYỆT ĐỀ XUẤT" Style="{StaticResource Text.Heading.Medium}" Margin="0,0,0,24"/>

        <!-- Stats summary -->
        <Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="16">
                <StackPanel>
                    <TextBlock Text="Đang chờ phê duyệt" Style="{StaticResource Text.Caption}" Foreground="{StaticResource Brush.Text.Muted}"/>
                    <TextBlock Text="5" Style="{StaticResource Text.Heading.Medium}" Foreground="#F39C12" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="16">
                <StackPanel>
                    <TextBlock Text="Đã phê duyệt (tháng này)" Style="{StaticResource Text.Caption}" Foreground="{StaticResource Brush.Text.Muted}"/>
                    <TextBlock Text="12" Style="{StaticResource Text.Heading.Medium}" Foreground="Green" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Background="White" CornerRadius="8" Padding="16">
                <StackPanel>
                    <TextBlock Text="Từ chối (tháng này)" Style="{StaticResource Text.Caption}" Foreground="{StaticResource Brush.Text.Muted}"/>
                    <TextBlock Text="2" Style="{StaticResource Text.Heading.Medium}" Foreground="Red" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Approval Process Interface -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: List -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Danh sách chờ duyệt" Style="{StaticResource Text.Body.Large}" FontWeight="Bold" Margin="0,0,0,16"/>

                    <DataGrid x:Name="dgPending" Grid.Row="1" Style="{StaticResource DataGrid}" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="dgPending_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="60"/>
                            <DataGridTextColumn Header="Người được ĐX" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Đơn vị" Binding="{Binding Unit}" Width="150"/>
                            <DataGridTextColumn Header="Ngày" Binding="{Binding Date}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Right: Action Form -->
            <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="16">
                <StackPanel>
                    <TextBlock Text="Chi tiết &amp; Quyết định" Style="{StaticResource Text.Body.Large}" FontWeight="Bold" Margin="0,0,0,16"/>

                    <StackPanel x:Name="pnlDetails" Visibility="Hidden">
                        <TextBlock x:Name="txtSubject" Text="ĐX: Huân chương chiến công cho Nguyễn Văn A" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,16"/>

                        <TextBlock Text="Ý KIẾN NGƯỜI PHÊ DUYỆT" Style="{StaticResource Text.Caption}" Margin="0,0,0,8"/>
                        <TextBox Height="100" AcceptsReturn="True" TextWrapping="Wrap" Padding="8" Margin="0,0,0,16"/>

                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Người phê duyệt" Style="{StaticResource Text.Caption}" Margin="0,0,0,8"/>
                            <ComboBox>
                                <ComboBoxItem Content="Trung tá Nguyễn Văn Hùng" IsSelected="True"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,24">
                            <TextBlock Text="Quyết định" Style="{StaticResource Text.Caption}" Margin="0,0,0,8"/>
                            <ComboBox x:Name="cboDecision">
                                <ComboBoxItem Content="Phê duyệt" IsSelected="True"/>
                                <ComboBoxItem Content="Từ chối"/>
                                <ComboBoxItem Content="Yêu cầu bổ sung"/>
                            </ComboBox>
                        </StackPanel>

                        <Button Content="CẬP NHẬT TRẠNG THÁI" Style="{StaticResource Button}" Tag="Primary" Height="40" Click="BtnUpdate_Click"/>
                    </StackPanel>

                    <TextBlock x:Name="txtEmptyState" Text="Vui lòng chọn một đề xuất từ danh sách để xem chi tiết." Foreground="{StaticResource Brush.Text.Muted}" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,40,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
