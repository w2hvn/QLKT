<UserControl x:Class="MilitaryRewardApp.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: User List -->
        <DataGrid x:Name="dgUsers" Grid.Column="0" Margin="0,0,10,0" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionChanged="dgUsers_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding UserID}" Width="50"/>
                <DataGridTextColumn Header="Tên đăng nhập" Binding="{Binding Username}" Width="150"/>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="200"/>
                <DataGridTextColumn Header="Quyền" Binding="{Binding Role}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Right: Form -->
        <StackPanel Grid.Column="1">
            <TextBlock Text="Quản lý người dùng" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
            
            <TextBlock Text="Tên đăng nhập:"/>
            <TextBox x:Name="txtUsername"/>
            
            <TextBlock Text="Mật khẩu:"/>
            <PasswordBox x:Name="txtPassword" Margin="0,5"/>
            
            <TextBlock Text="Họ tên hiển thị:"/>
            <TextBox x:Name="txtFullName"/>
            
            <TextBlock Text="Quyền hạn:"/>
            <ComboBox x:Name="cboRole" Margin="0,5">
                <ComboBoxItem Content="Admin"/>
                <ComboBoxItem Content="User"/>
            </ComboBox>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Thêm" Click="Add_Click" Margin="0,0,10,0" Width="60"/>
                <Button Content="Sửa" Click="Update_Click" Margin="0,0,10,0" Width="60"/>
                <Button Content="Xóa" Click="Delete_Click" Background="Red" Width="60"/>
            </StackPanel>
            
            <Button Content="Làm mới" Click="Clear_Click" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
