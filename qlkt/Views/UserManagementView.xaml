<UserControl x:Class="QLKT.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="QUẢN LÝ NGƯỜI DÙNG" Style="{StaticResource Text.Heading.Medium}" Margin="0,0,0,24"/>

        <!-- Actions -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBox Width="250" Padding="8,4" VerticalContentAlignment="Center" Margin="0,0,16,0" Text="Tìm kiếm người dùng..."/>
                    <ComboBox Width="150" SelectedIndex="0" Padding="8,4" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Tất cả vai trò"/>
                        <ComboBoxItem Content="Quản trị viên"/>
                        <ComboBoxItem Content="Người dùng"/>
                    </ComboBox>
                </StackPanel>
                <Button Grid.Column="1" Content="+ Thêm người dùng" Style="{StaticResource Button}" Tag="Primary" Padding="16,8"/>
            </Grid>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="16">
            <DataGrid x:Name="dgUsers" Style="{StaticResource DataGrid}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã NV" Binding="{Binding ID}" Width="80"/>
                    <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Tên đăng nhập" Binding="{Binding Username}" Width="150"/>
                    <DataGridTextColumn Header="Vai trò" Binding="{Binding Role}" Width="150"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="100"/>
                    <DataGridTemplateColumn Header="Thao tác" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✏️" Margin="0,0,10,0" Cursor="Hand" ToolTip="Sửa"/>
                                    <TextBlock Text="🗑️" Cursor="Hand" ToolTip="Xóa" Foreground="Red"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
